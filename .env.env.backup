# ArXiv 论文追踪器环境变量配置模板
# 使用方法：
# 1. 复制此文件为 .env：cp env.example .env
# 2. 填写您的真实配置信息
# 3. .env 文件不会被上传到 GitHub（已在 .gitignore 中排除）

# =============================================================================
# AI API 配置
# =============================================================================

# DeepSeek AI API 密钥 (必需)
# 获取地址: https://platform.deepseek.com/
# 格式: sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
DEEPSEEK_API_KEY=sk-4cd66b1e7df74c06a094e67657facd1f

# =============================================================================
# 邮件服务配置
# =============================================================================

# SMTP 服务器地址 (必需)
# 推荐使用 Gmail（配置简单，稳定性好）
# Gmail: smtp.gmail.com（推荐）
# QQ邮箱: smtp.qq.com
# 163邮箱: smtp.163.com  
# Outlook: smtp-mail.outlook.com
SMTP_SERVER=smtp.gmail.com

# SMTP 服务器端口 (可选，默认587)
# TLS: 587, SSL: 465
SMTP_PORT=587

# 邮箱账号 (必需)
# Gmail: your-email@gmail.com（推荐）
# QQ邮箱: your-email@qq.com
SMTP_USERNAME=at0rp1d0i@gmail.com

# 邮箱授权码 (必需，不是登录密码！)
# Gmail: 应用专用密码（需要启用两步验证）
#   - 访问 https://myaccount.google.com/security
#   - 启用两步验证
#   - 生成应用专用密码（格式：abcd efgh ijkl mnop）
# QQ邮箱: 设置 → 账户 → POP3/IMAP/SMTP → 生成授权码
# 163邮箱: 设置 → POP3/SMTP/IMAP → 设置客户端授权密码
SMTP_PASSWORD=ayln qkds sqru vuiy

# 发件人邮箱 (必需，通常与SMTP_USERNAME相同)
EMAIL_FROM=at0rp1d0i@gmail.com

# 收件人邮箱 (必需)
# 可以设置为您自己的邮箱进行测试
# 单个邮箱: recipient@example.com
# 多个邮箱: user1@example.com,user2@example.com,user3@example.com
EMAIL_TO=at0rp1d0i@gmail.com

# =============================================================================
# 可选配置 (有默认值，通常不需要修改)
# =============================================================================

# 论文搜索类别 (默认: cs.AI,cs.LG,cs.CL)
# CATEGORIES=cs.AI,cs.LG,cs.CL

# 最大论文数量 (默认: 50)
# MAX_PAPERS=50

# 搜索最近几天的论文 (默认: 2)
# SEARCH_DAYS=2

# AI 模型名称 (默认: deepseek-chat)
# AI_MODEL=deepseek-chat

# API 重试次数 (默认: 3)
# API_RETRY_TIMES=3

# API 调用间隔秒数 (默认: 2)
# API_DELAY=2 